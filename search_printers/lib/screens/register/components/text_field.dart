/*
import 'package:flutter/material.dart';

class  CustomTextField extends StatelessWidget {
  const CustomTextField({super.key});

  @override
  Widget build(BuildContext context){
    return const Scaffold(
      body: Padding(
        padding: EdgeInsets.all(8),
        child: Column(
          children: [
            TextField(decoration: InputDecoration(labelText: 'Nome da Impressora')),
            TextField(decoration: InputDecoration(labelText: 'Marca')),
            TextField(decoration: InputDecoration(labelText: 'Cor(Preto ou Colorido)')),
            TextField(decoration: InputDecoration(labelText: 'Velocidade preto e branco')),
            TextField(decoration: InputDecoration(labelText: 'Velocidade colorido*')),
            TextField(decoration: InputDecoration(labelText: 'Funções')),
            TextField(decoration: InputDecoration(labelText: 'Alimentação')),
            TextField(decoration: InputDecoration(labelText: 'Qualidade de impressão preto e branco')),
            TextField(decoration: InputDecoration(labelText: 'Qualidade de impressão colorida*')),
          ],
        ),
      ),
    );
  }
}
*/